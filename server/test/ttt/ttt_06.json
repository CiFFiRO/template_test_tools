{"type":1,"rules":{"p":["cerr","cout"]},"testText":"#include <unistd.h>\n#include <iostream>\n\nint main() {\n  std::${p} << 'q';\n  fork();\n  return 0;\n}\n\nПри запуске программы выше, количество букв q будет выведено в терминале ","answerScript":"let options = ['Одна', 'Две', 'Будет ошибка времени выполнения', \n'Будет ошибка времени компиляции',  'Ничего не будет выведено'];\n$$AnswerIs([options[$p]]);\noptions.splice($p, 1);\n$$FalseOptionsIs($$rSubArray(options, options.length));"}